# Copier en .env pour du developpement local (jamais commiter le .env reel)

MONGO_URI=mongodb://user:password@host:port/dbname
MONGO_DB_NAME=pulse_benin

# Connexion directe fonctionne de maniere fiable (confirme par AUTOMATIONS-BI
# et par lka-bi-pipeline lui-meme depuis GitHub Actions). Le tunnel WireGuard
# (10.66.66.1, config recue separement) reste disponible en secours si la
# connexion directe redevient instable.
MYSQL_HOST=75.119.154.255
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=
MYSQL_DATABASE=lka_bi_dw
